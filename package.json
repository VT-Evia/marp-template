{
  "name": "marp-presentations",
  "version": "1.0.0",
  "description": "Marp-based presentation template with Codespace support",
  "scripts": {
    "build": "mkdir -p output && marp --input-dir presentations/ --output output/",
    "build:pdf": "mkdir -p output && marp --input-dir presentations/ --output output/ --pdf --allow-local-files",
    "build:pptx": "mkdir -p output && marp --input-dir presentations/ --output output/ --pptx",
    "build:all": "npm run build && npm run build:pdf && npm run build:pptx",
    "watch": "marp --input-dir presentations/ --output output/ --watch",
    "preview": "marp --input-dir presentations/ --output output/ --preview --watch"
  },
  "devDependencies": {
    "@marp-team/marp-cli": "^3.4.0"
  }
}